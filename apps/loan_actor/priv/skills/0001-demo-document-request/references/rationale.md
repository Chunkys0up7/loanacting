# Rationale

This is a demonstration reference file, proving that a skill pack can
bundle more than just `SKILL.md` (`contracts/skill-format.md`). Foundation
does not implement real compliance/escalation skill semantics — subsequent
intents will author packs with actual business rules and link them to the
tests that prove they fire, per constitution Principle VI.
